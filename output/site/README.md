# Japan 2027 — site artifact

- `v1/` — зафиксированная исходная версия.
- `v2/` — motion edition: расширенная анимация и интерактивность при сохранённом дизайне.
- `index.html` и `assets/` в корне — исходный снимок до версионирования.

Обе версии автономны: каждая открывается собственным `index.html` и использует локальный hero-asset.
